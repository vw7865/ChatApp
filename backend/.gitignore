node_modules
dist
.env
data/sessions/*
!data/sessions/.gitkeep
# Local instance metadata (tokens / IDs); never commit
data/inst_*.json
# Legacy / local Baileys dirs (sessions now live under DATA_DIR per instance)
sessions/
